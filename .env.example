# Application
APP_NAME=Audiobook Generator
APP_ENV=development
DEBUG=true
HOST=127.0.0.1
PORT=8000

# Paths (relative to project root)
DATABASE_URL=sqlite:///./storage/audiobook.db
STORAGE_ROOT=./storage
FRONTEND_DIR=./frontend

# Logging
LOG_LEVEL=INFO
LOG_FILE=./storage/logs/app.log

# API (future integration — do not commit real keys)
# GEMINI_API_KEY=
